# .env.example
# 実際の値は入れずに Git にコミットする。ローカル検証時は .env にコピーして値を記入する。

# Gemini API（FR-02：要約・翻訳・分類）
GEMINI_API_KEY=your_gemini_api_key_here

# Discord Webhook（FR-03：通知）
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# GITHUB_TOKEN は GitHub Actions が自動提供するため定義不要
